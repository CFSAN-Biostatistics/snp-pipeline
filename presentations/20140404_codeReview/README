#One way to generate the slides from markdown:
landslide -i -d codeReview.html codeReview.md

#One way to generate pdf from markdown:
landslide codeReview.md -d codereview.pdf
#can also print from firefox, which seems to be somewhat better